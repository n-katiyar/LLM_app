import { createClasses } from "@hitachivantara/uikit-react-utils";
const { staticClasses, useClasses } = createClasses("HvListContainer", {
  root: {
    overflow: "clip",
    overflowClipMargin: 4
  }
});
export {
  staticClasses,
  useClasses
};
