import { jsx } from "react/jsx-runtime";
import { useDefaultProps } from "@hitachivantara/uikit-react-utils";
import { Bar } from "./Bar.js";
import { HvHeaderMenuItem } from "../MenuItem/MenuItem.js";
const HvHeaderMenuBar = (props) => {
  const {
    data = [],
    onClick,
    type = "menubar",
    levels,
    currentLevel,
    classes,
    ...others
  } = useDefaultProps("HvHeaderMenuBar", props);
  return /* @__PURE__ */ jsx(Bar, { data, type, classes, ...others, children: data.map((item) => /* @__PURE__ */ jsx(
    HvHeaderMenuItem,
    {
      item,
      type,
      onClick,
      levels,
      currentLevel
    },
    item.id
  )) });
};
export {
  HvHeaderMenuBar
};
