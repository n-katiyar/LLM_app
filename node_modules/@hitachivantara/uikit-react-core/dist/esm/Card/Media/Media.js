import { jsx } from "react/jsx-runtime";
import MuiCardMedia from "@mui/material/CardMedia";
import { useClasses } from "./Media.styles.js";
import { staticClasses } from "./Media.styles.js";
const HvCardMedia = ({
  classes: classesProp,
  className,
  children,
  ...others
}) => {
  const { classes } = useClasses(classesProp);
  return /* @__PURE__ */ jsx(
    MuiCardMedia,
    {
      classes: {
        root: classes.root,
        media: classes.media
      },
      className,
      role: "img",
      ...others,
      children
    }
  );
};
export {
  HvCardMedia,
  staticClasses as cardMediaClasses
};
