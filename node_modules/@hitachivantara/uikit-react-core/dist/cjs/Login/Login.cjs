"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("react/jsx-runtime");
const React = require("react");
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const Login_styles = require("./Login.styles.cjs");
const HvLogin = React.forwardRef(function HvLogin2(props, ref) {
  const {
    className,
    classes: classesProp,
    children,
    background,
    ...others
  } = uikitReactUtils.useDefaultProps("HvLogin", props);
  const { classes, cx } = Login_styles.useClasses(classesProp);
  return /* @__PURE__ */ jsxRuntime.jsx(
    "div",
    {
      ref,
      className: cx(classes.root, className),
      style: {
        backgroundImage: background && `url(${background})`
      },
      ...others,
      children: /* @__PURE__ */ jsxRuntime.jsx("div", { className: classes.formContainer, children })
    }
  );
});
exports.loginClasses = Login_styles.staticClasses;
exports.HvLogin = HvLogin;
