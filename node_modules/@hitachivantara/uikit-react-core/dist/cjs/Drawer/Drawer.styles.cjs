"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const uikitStyles = require("@hitachivantara/uikit-styles");
const { staticClasses, useClasses } = uikitReactUtils.createClasses("HvDrawer", {
  root: {},
  paper: {
    backgroundColor: uikitStyles.theme.colors.atmo1,
    padding: 0,
    overflow: "auto",
    boxShadow: uikitStyles.theme.colors.shadow
  },
  background: {
    background: uikitStyles.theme.alpha("atmo4", 0.8)
  },
  closeButton: {
    position: "absolute",
    top: uikitStyles.theme.spacing("sm"),
    right: uikitStyles.theme.spacing("sm")
  }
});
exports.staticClasses = staticClasses;
exports.useClasses = useClasses;
