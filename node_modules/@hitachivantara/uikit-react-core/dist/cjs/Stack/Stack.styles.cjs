"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const uikitStyles = require("@hitachivantara/uikit-styles");
const { staticClasses, useClasses } = uikitReactUtils.createClasses("HvStack", {
  root: {
    display: "flex"
  },
  column: {
    flexDirection: "column"
  },
  row: {
    flexDirection: "row"
  },
  divider: {
    borderColor: uikitStyles.theme.colors.atmo4
  },
  xs: {
    gap: uikitStyles.theme.space.xs
  },
  sm: {
    gap: uikitStyles.theme.space.sm
  },
  md: {
    gap: uikitStyles.theme.spacing(4)
  },
  lg: {
    gap: uikitStyles.theme.spacing(6)
  },
  xl: {
    gap: uikitStyles.theme.spacing(11)
  }
});
exports.staticClasses = staticClasses;
exports.useClasses = useClasses;
