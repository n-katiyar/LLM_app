"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const uikitStyles = require("@hitachivantara/uikit-styles");
const { staticClasses, useClasses } = uikitReactUtils.createClasses("HvSuggestions", {
  root: { position: "relative" },
  list: {
    backgroundColor: uikitStyles.theme.colors.atmo1,
    border: `1px solid ${uikitStyles.theme.colors.secondary}`,
    boxShadow: uikitStyles.theme.colors.shadow,
    padding: uikitStyles.theme.space.xs,
    width: "100%"
  },
  popper: {
    width: "var(--popper-width)",
    position: "absolute",
    zIndex: uikitStyles.theme.zIndices.tooltip,
    ":not($portal)": {
      transform: "translate3d(0, -1px, 0) !important"
    }
  },
  portal: {}
});
exports.staticClasses = staticClasses;
exports.useClasses = useClasses;
