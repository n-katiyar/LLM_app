"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const styles = require("@mui/material/styles");
const useMediaQuery = require("@mui/material/useMediaQuery");
const uikitStyles = require("@hitachivantara/uikit-styles");
const _interopDefault = (e) => e && e.__esModule ? e : { default: e };
const useMediaQuery__default = /* @__PURE__ */ _interopDefault(useMediaQuery);
const useWidth = () => {
  const muiTheme = styles.useTheme();
  const keys = Object.keys(uikitStyles.theme.breakpoints.values).reverse();
  return keys.reduce((output, key) => {
    const matches = useMediaQuery__default.default(muiTheme.breakpoints.up(key));
    return !output && matches ? key : output;
  }, null) || "xs";
};
exports.useWidth = useWidth;
