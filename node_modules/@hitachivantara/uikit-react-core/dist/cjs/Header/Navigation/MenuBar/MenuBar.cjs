"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("react/jsx-runtime");
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const Bar = require("./Bar.cjs");
const MenuItem = require("../MenuItem/MenuItem.cjs");
const HvHeaderMenuBar = (props) => {
  const {
    data = [],
    onClick,
    type = "menubar",
    levels,
    currentLevel,
    classes,
    ...others
  } = uikitReactUtils.useDefaultProps("HvHeaderMenuBar", props);
  return /* @__PURE__ */ jsxRuntime.jsx(Bar.Bar, { data, type, classes, ...others, children: data.map((item) => /* @__PURE__ */ jsxRuntime.jsx(
    MenuItem.HvHeaderMenuItem,
    {
      item,
      type,
      onClick,
      levels,
      currentLevel
    },
    item.id
  )) });
};
exports.HvHeaderMenuBar = HvHeaderMenuBar;
