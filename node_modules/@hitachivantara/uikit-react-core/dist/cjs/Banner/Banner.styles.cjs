"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const uikitReactUtils = require("@hitachivantara/uikit-react-utils");
const uikitStyles = require("@hitachivantara/uikit-styles");
const { useClasses, staticClasses } = uikitReactUtils.createClasses("HvBanner", {
  /** Styles applied to the component root class when the component is closed. */
  rootClosed: {
    display: "none"
  },
  /** Styles applied to the component root class. */
  root: {
    minWidth: `calc(100% - ${uikitStyles.theme.space.sm})`
  },
  /** Styles applied to the component when define as top. */
  anchorOriginTopCenter: {
    top: uikitStyles.theme.space.xs
  },
  /** Styles applied to the component when define as bottom. */
  anchorOriginBottomCenter: {
    bottom: uikitStyles.theme.space.xs
  }
});
exports.staticClasses = staticClasses;
exports.useClasses = useClasses;
